#include "stdafx.h"
#include <iostream>
#include <vector>
using namespace std;
int _tmain(int argc, _TCHAR* argv[]) {
	vector<int> a;
	a.push_back(1);
	a.push_back(2);
	a.push_back(3);
	a.push_back(4);
	for (auto x : a) {
		cout << x << endl;
	}
	for (int i = 0; i < a.size(); i++){
		cout << a[i] << endl;
	}
	a.pop_back();	// 3ŒÂ‚É‚È‚é
	a.pop_back();	// 2ŒÂ‚É‚È‚é
	for (auto x : a){
		cout << x << endl;
	}
	
	return 0;
}

