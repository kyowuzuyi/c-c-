#include "stdafx.h"
#include <iostream>
#include <vector>
using namespace std;

template <class T>
class Player {
public:
	T x;
	Player(T x) : x(x) {}
	~Player() { std::cout << "destructor\n"; }
};

void func(const Player<int >& p) // ƒRƒs[‚ª”­¶‚·‚é
{
	// p.x = 99;
}

int _tmain(int argc, _TCHAR* argv[])
{
	Player<int> p(10);
	func(p);
	cout << p.x << endl;

	return 0;
}

